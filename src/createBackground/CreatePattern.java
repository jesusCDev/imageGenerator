package createBackground;

import java.util.Random;

public class CreatePattern {

	/*
	 * This will allow you to create a patter in the backgorund by using
	 * algorithms to create them
	 */
	public void getBackgroundShapePattern(){
		Random randomNumber = new Random();
		int randomFormulaPicked = randomNumber.nextInt();
		FormulaShape formulaShape = new FormulaShape();
		
		
	}
	
	/*
	 * This will get the size between how big the shape should be
	 * this should have a restriction on size
	 */
	public void getBackgroundShapeSize(){
		
	}
	
	/*
	 * this will allow you to get to any shape
	 */
	public void getBackgroundShapeAngle(){
		
	}
	
	/*
	 * this will retun the shape of the main shape
	 * size will be the same for all of them
	 */
	public void getMainShape(){
		
	}
}
